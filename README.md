# Spam_Email_Detector
The Spam Email Detector is a machine learning-based system that classifies messages as spam or not spam using NLP techniques like TF-IDF and the Naive Bayes algorithm. It runs in a simple console environment and provides quick, accurate results without internet access.
